additionalProperties:
  formFields:
    - default: "9000"
      envKey: PANEL_APP_PORT_HTTP
      label:
        en: Port
        zh: 端口
      required: true
      type: number
      edit: true
      rule: paramPort
    - default: http://localhost
      envKey: PANEL_APP_BASE_URL
      label:
        en: localhost
        zh: Mealie 后台访问地址
      required: true
      type: text
      edit: true
      rule: paramExtUrl
      values: []